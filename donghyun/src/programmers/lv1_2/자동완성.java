package programmers.lv1_2;

import java.io.*;
import java.util.*;

public class 자동완성 {
    int result;

    public int solution(String[] words) {
        int answer = 0;
        return answer;

    }

    private int getResult(String[] words) throws Exception { // words 배열 안에 단어들이 들어있음
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        //Stack<Character> stack = new Stack<>();

        for (int i = 0; i < words.length; i++) {
            //stack.push(words[0].charAt(i));
            char[] answer2 = words[0].toCharArray();
        }
        return 0;
    }
}
